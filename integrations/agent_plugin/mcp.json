{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "quotabot": {
      "type": "stdio",
      "command": "quotabot",
      "args": ["mcp"],
      "cwd": "${PLUGIN_DATA}"
    }
  }
}
