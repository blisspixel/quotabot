name: quotabot_collector
description: Collects AI subscription quota/usage across providers into one normalized JSON feed.
version: 0.8.0
environment:
  sdk: ^3.6.0

dependencies:
  http: ^1.6.0
  sqlite3: ^3.4.0
  mcp_dart: ^2.2.2
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
