import 'package:quotabot_collector/mcp_server_entrypoint.dart';

/// Standalone source entrypoint, also available in the CLI as `quotabot mcp`.
Future<void> main(List<String> args) => runQuotabotMcpServer(args);
